@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width"/>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css"/>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.6/angular.min.js"></script>
    <title>Index</title>
</head>
<body>

<div class="container" ng-app="challenge">
    <div ng-controller="ChallengeController as challengeCtrl" ng-cloak>
        <div class="page-header">
            <h1>Code Challenge</h1>
            <p class="lead">
                Use AngularJs to submit form data and fix the <b>Color</b> label position.
            </p>
            <p class="lead">Write WebAPI and associated server-side logic to capture the form data and display the data on a new page. You don't need to save it to the database</p>
        </div>
        <div class="row">
            <form method="post" action="/" class="col-md-4" ng-submit="challengeCtrl.submitFn($event)">
                <div class="form-group">
                    <label>
                        Your Name
                    </label>
                    <input type="text" class="form-control" ng-model="challengeCtrl.name"/>
                </div>
                <div>
                    <label>
                        Color
                    </label>
                </div>
                <div class="radio form-group">
                    <label>
                        <input type="radio" ng-model="challengeCtrl.color" value ="Red" name="color"/>
                        Red
                    </label>
                    <label>
                        <input type="radio" ng-model="challengeCtrl.color" value="Green" name="color"/>
                        Green
                    </label>
                    <label>
                        <input type="radio" ng-model="challengeCtrl.color" value="Blue" name="color"/>
                        Blue
                    </label>
                </div>
                <div class="form-group">
                    <label>
                        Day
                    </label>
                    <select ng-model="challengeCtrl.weekday"
                            class="form-control" 
                            ng-options="day.long as day.short for day in challengeCtrl.weekdayOptions">
                    </select>
                </div>
                <div class="form-group">
                    <input type="submit" value="Submit" class="btn btn-info"/>
                </div>
            </form>
        </div>
        <div class="alert alert-info" ng-if="challengeCtrl.success">
            <strong>Success!</strong>
            <div>
                Data successfully submitted! @Html.ActionLink("Click here to view", "ChallengeData")
            </div>
        </div>
    </div>
</div>
<script src="/scripts/app.js"></script>
</body>
</html>